#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    vector<int>v;
    for(int i=0;i<n;i++)
    {
        int x;cin>>x;
    }
    vector<int>pref(n,0);
    vector<int>suff(n,0);
    if(v[0]==0)
    pref[0]=1;
    else
    pref[0]=0;
    for(int i=0;i<n;i++)
    {
     
    }
}