#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    string check = "vika";
    int n, m;
    cin >> n >> m;
    while (n--)
    {
        for (int i = 0; i < n; i++)
        {
            string s;
            cin >> s;
        }
    }
    for (int i = 0; i < 4; i++)
    {
        for (int j = 0; j < m; j++)
        {string f;
            for (int i = 0; i < n; i++)
            {f=f+s[j][i]
            }
        }
    }
}