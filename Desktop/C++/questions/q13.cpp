#include<bits/stdc++.h>
using namespace std;
int main()
{
    int x;
    cin>>x;
    while(x--)
    {
        int n,k;
        cin>>n>>k;
        vector<int>v1;
        vector<int>v2;
        vector<int>v3;
        int a=v1.size();
        for(int i:v1)
        {cin>>i;
        v3[i]=v1[i];}
        for(int i:v2)
        cin>>i;
        vector<int>v4;
        for(int i=0;i<a;i++)
        {
            v4.push_back(i);
        }
        sort(v1.begin(),v1.end());
        sort(v2.begin(),v2.end());
        sort(v
    }
}