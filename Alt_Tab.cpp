#include <bits/stdc++.h>
using namespace std;

int main()
{

    string u;
    int n;
    cin >> n;
    int b = n;
    vector<string> v(n);

    for (int i = 0; i < n; i++)
    {
        string s;
        cin >> s;
        v.push_back(s);
        reverse(s.begin(), s.end());
for (size_t i = 0; i < count; i++)
{
    /* code */
}

          s.erase(2,n);
        
 reverse(s.begin(),s.end());
 cout<<s<<endl;
    }

    return 0;
}
