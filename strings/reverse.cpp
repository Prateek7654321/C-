#include<bits/stdc++.h>
using namespace std;
int main()
{
    // string s="abcdefgh";
    // // reverse first half
    // int a =s.size();
    // reverse(s.begin(),s.begin()+a/2);
    // cout<<s;
  std::vector<int> v(6);
 
    std::iota(v.begin(), v.end(), 1);
 for(int i=0;i<6;i++)
 cout<<v[i]<<" ";


    
}